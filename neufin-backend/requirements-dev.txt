# Development-only dependencies — not installed in the production Docker image.
# Install with: pip install -r requirements-dev.txt
#
# Usage:
#   pytest tests/unit/ -v
#   black neufin-backend/
#   ruff check neufin-backend/

# Testing
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-cov==6.1.0

# Formatting & linting
black==25.1.0
ruff==0.11.2
pytest-timeout==2.3.1

# HTTP test client (also in requirements.txt; explicit here for IDE completion)
httpx==0.28.1
